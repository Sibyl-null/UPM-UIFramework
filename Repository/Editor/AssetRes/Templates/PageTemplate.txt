{{~ for $namespace in dep_namespace_set ~}}
using {{ $namespace }};
{{~ end ~}}

namespace {{ self_namespace }}
{
    public class {{ page_class_name }} : BasePage
    {
        private {{ ui_class_name }} _ui;
        
        protected override void OnInit()
        {
            base.OnInit();
            _ui = ({{ ui_class_name }})BaseUI;
        }

        protected override void OnPrepare(IPageArg arg = null)
        {
            base.OnPrepare(arg);
        }

        protected override void OnClear()
        {
            base.OnClear();
        }
    }
}